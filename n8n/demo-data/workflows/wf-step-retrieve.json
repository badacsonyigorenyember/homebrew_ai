{
  "updatedAt": "2026-08-20T05:14:27.122Z",
  "createdAt": "2026-08-19T18:36:02.461Z",
  "id": "cCt9O4NyNPyegHBq",
  "name": "wf-step-retrieve",
  "description": "Shared retrieval step: one query in, ranked passages out. Embeds the query with bge-m3, then calls nlq.search_knowledge (hybrid, RRF) and logs the hit list to obs.retrievals. Returns one of two shapes depending on the `mode` input — `text` gives the model a single [S1]..[Sn] labelled block, `rows` gives a capability the structured rows with chunk_id. Called by chat-agent's search_brewing_knowledge tool and by cap-brainstorm-pairing steps 2a and 2b.",
  "active": true,
  "isArchived": false,
  "nodes": [
    {
      "parameters": {
        "workflowInputs": {
          "values": [
            {
              "name": "query"
            },
            {
              "name": "top_k",
              "type": "number"
            },
            {
              "name": "mode"
            },
            {
              "name": "session_id"
            }
          ]
        }
      },
      "type": "n8n-nodes-base.executeWorkflowTrigger",
      "typeVersion": 1.2,
      "position": [
        0,
        0
      ],
      "id": "a4101c88-485b-4637-a8d2-5e5bc0782a5b",
      "name": "When Executed by Another Workflow"
    },
    {
      "parameters": {
        "jsCode": "const i = $input.first().json;\n\nconst query = String(i.query ?? '').trim();\nif (!query) throw new Error('wf-step-retrieve called with an empty query');\n\n// kb.documents.doc_type allows book|style_guide|article|datasheet|note.\n// Anything else means the caller guessed, so drop it to '' (= whole corpus).\n// `note` stays out on purpose: kb.documents has 0 rows of it (measured 2026-09-14).\nconst allowed = ['book', 'style_guide', 'article', 'datasheet'];\nconst dt = String(i.doc_type ?? '').trim().toLowerCase();\n\nconst k = Number(i.top_k);\n\n// ⛔ `mode` MUST be carried through — `Return rows` reads it from this node, not\n// from the trigger. Dropping it here silently forces the text shape, and every\n// capability that asked for `rows` gets an empty array.\nconst mode = String(i.mode ?? 'text').trim().toLowerCase() === 'rows' ? 'rows' : 'text';\n\nreturn [{ json: {\n  query,\n  mode,\n  // Carried for the retrieval trace only; never used in the search itself.\n  session_id: String(i.session_id ?? '').trim(),\n  doc_type: allowed.includes(dt) ? dt : '',\n  top_k: Number.isFinite(k) && k > 0 ? Math.min(Math.round(k), 8) : 6,\n}}];"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        208,
        0
      ],
      "id": "25193fc8-6164-4612-8ed5-0d5412a0e883",
      "name": "Normalise input",
      "notes": "The only validation point on the retrieval path. ⛔ `mode` MUST be carried through here: `Return rows` reads it from THIS node, not from the trigger, so dropping it silently forces the text shape and every capability that asked for `rows` gets an empty array. doc_type is fail-open — an unrecognised value becomes '' and NULLIF turns that into a whole-corpus search, so a typo costs scope, not an error. top_k is clamped to 1..8, default 6."
    },
    {
      "parameters": {
        "method": "POST",
        "url": "http://ollama:11434/api/embed",
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ { \"model\": \"bge-m3\", \"input\": $json.query, \"keep_alive\": -1 } }}",
        "options": {
          "timeout": 120000
        }
      },
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.4,
      "position": [
        416,
        0
      ],
      "id": "961dd4e1-313b-4364-8de9-e8b4f1b081fc",
      "name": "Embed query"
    },
    {
      "parameters": {
        "jsCode": "const src  = $('Normalise input').first().json;\nconst mode = String(src.mode ?? 'text').toLowerCase() === 'rows' ? 'rows' : 'text';\n\n// Always Output Data is ON upstream, so a zero-row search arrives as one empty\n// item. Filtering on chunk_id is what turns that back into \"no results\".\nconst raw = $('Search knowledge').all().map(i => i.json).filter(r => r.chunk_id != null);\n\n// Words in the question the corpus vocabulary has never seen, after the\n// typo/locale and off-domain guards (nlq.corpus_vocabulary_gap). One value per\n// call, repeated on every row by the search query.\n//\n// ⛔ Deliberately NOT added to the `text` shape. The model gaining a new fact it\n// cannot act on -- there is no web tool yet -- can only make it hedge, and that\n// is a grounding regression bought for nothing. Until something consumes the\n// flag it is for capabilities and for obs.retrievals.gap_terms, which is the\n// evidence that decides whether the consumer is worth building.\nconst gap = $('Search knowledge').first().json.gap_terms ?? [];\n\nconst rows = raw.map(r => ({\n  chunk_id: Number(r.chunk_id),\n  doc_title: r.doc_title,\n  heading: Array.isArray(r.heading_path)\n    ? r.heading_path.join(' > ')\n    : String(r.heading_path ?? ''),\n  page: r.page_from ?? null,\n  text: String(r.raw_content).replace(/\\s+/g, ' ').trim(),\n}));\n\n// Structured shape — for capabilities. Empty is a valid answer, not an error;\n// the caller decides whether zero results is fatal.\nif (mode === 'rows') {\n  return [{ json: { query: src.query, count: rows.length, gap, rows } }];\n}\n\n// Text shape — for the model. ONE item, ONE key.\nif (!rows.length) {\n  return [{ json: { response: `No passages in the library matched \"${src.query}\".` } }];\n}\n\nconst body = rows.map((x, n) =>\n  `[S${n + 1}] ${x.doc_title} · ${x.heading}${x.page ? ` · p.${x.page}` : ''}\\n${x.text}`\n).join('\\n\\n');\n\nreturn [{ json: {\n  response: `${rows.length} passages from the library for \"${src.query}\":\\n\\n${body}`,\n}}];"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1040,
        0
      ],
      "id": "7d4c4704-2c3e-46eb-8d16-2dc2beb75a66",
      "name": "Return rows",
      "notes": "Two output shapes from one node, chosen by `mode`. `rows` returns the structured rows for a capability, where zero results is a valid answer and the caller decides if that is fatal; `text` returns ONE item with ONE `response` key, because the toolWorkflow node hands the model only the last output — this is where the [S1]..[Sn] labels the system prompt cites are actually minted, and the page suffix is null-safe for page-less sources. The chunk_id filter is load-bearing: Always Output Data is ON upstream, so a zero-row search arrives as one empty item rather than none."
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "-- The gap CTE is the DRIVING side of the join and always yields exactly one row,\n-- so the flag survives even a zero-row search. Not that one is reachable:\n-- nlq.search_knowledge's vector arm has no relevance predicate, so it always\n-- returns p_limit rows -- which is precisely why \"the corpus found nothing\" is\n-- not an available signal and this function exists instead.\nWITH g AS (SELECT * FROM nlq.corpus_vocabulary_gap($1::text))\nSELECT s.chunk_id, s.doc_title, s.heading_path, s.page_from, s.raw_content,\n       g.gap_terms\nFROM g\nLEFT JOIN LATERAL nlq.search_knowledge(\n  $1::text,\n  $2::vector,\n  $3::int,\n  40,\n  50,\n  'bge-m3',\n  NULLIF($4::text, '')\n) s ON true;",
        "options": {
          "queryReplacement": "={{ [$json.query, $json.embedding, $json.top_k, $json.doc_type] }}"
        }
      },
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.6,
      "position": [
        832,
        0
      ],
      "id": "d4b1e463-1fca-458d-a31c-9c090c99bbf8",
      "name": "Search knowledge",
      "credentials": {
        "postgres": {
          "id": "6vilCQCI30RUdvfd",
          "name": "n8n_agent"
        }
      },
      "alwaysOutputData": true
    },
    {
      "parameters": {
        "jsCode": "const src = $('Normalise input').first().json;\nconst emb = $json.embeddings?.[0];\n\nif (!Array.isArray(emb) || emb.length !== 1024) {\n  throw new Error(`Expected a 1024-dim embedding, got ${emb?.length} — is bge-m3 loaded?`);\n}\n\n// pgvector literal, cast with $2::vector downstream. A JS array would arrive as a\n// Postgres array, not a vector.\nreturn [{ json: { ...src, embedding: '[' + emb.join(',') + ']' } }];"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        624,
        0
      ],
      "id": "851e43ce-e7cf-4eae-ab36-422aa45ef867",
      "name": "Build vector"
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "-- $6 arrives as a JSON array and is parsed here rather than assembled as a\n-- Postgres array literal in the expression: lexemes are arbitrary text, and\n-- JSON.stringify already escapes what a bare '{a,b}' literal would not.\nSELECT obs.f_log_retrieval($1::text, $2::text, $3::bigint[], $4::int, $5::text,\n       ARRAY(SELECT jsonb_array_elements_text($6::jsonb))) AS retrieval_id;",
        "options": {
          "queryReplacement": "={{ [$('Normalise input').first().json.session_id, $('Normalise input').first().json.query, '{' + $('Search knowledge').all().map(i => i.json.chunk_id).filter(v => v !== null && v !== undefined).join(',') + '}', $('Normalise input').first().json.top_k, $('Normalise input').first().json.mode, JSON.stringify($('Search knowledge').first().json.gap_terms ?? [])] }}"
        }
      },
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.6,
      "position": [
        1040,
        0
      ],
      "id": "c1f0a7d2-3b44-4e91-9a10-7f2c5e8d1b60",
      "name": "Log retrieval",
      "credentials": {
        "postgres": {
          "id": "memWriterCred001",
          "name": "Postgres — mem_writer"
        }
      },
      "executeOnce": true,
      "alwaysOutputData": true,
      "onError": "continueRegularOutput"
    }
  ],
  "connections": {
    "When Executed by Another Workflow": {
      "main": [
        [
          {
            "node": "Normalise input",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Normalise input": {
      "main": [
        [
          {
            "node": "Embed query",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Embed query": {
      "main": [
        [
          {
            "node": "Build vector",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Search knowledge": {
      "main": [
        [
          {
            "node": "Log retrieval",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Build vector": {
      "main": [
        [
          {
            "node": "Search knowledge",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Log retrieval": {
      "main": [
        [
          {
            "node": "Return rows",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "settings": {
    "executionOrder": "v1",
    "binaryMode": "separate"
  },
  "staticData": null,
  "meta": {
    "templateCredsSetupCompleted": true
  },
  "nodeGroups": [],
  "pinData": {
    "When Executed by Another Workflow": [
      {
        "json": {
          "query": "what temperature and how long for a diacetyl rest",
          "top_k": 6
        }
      }
    ]
  },
  "versionId": "e561a6dc-6053-4172-9871-231553105929",
  "activeVersionId": "e561a6dc-6053-4172-9871-231553105929",
  "versionCounter": 41,
  "triggerCount": 0,
  "tags": [],
  "shared": [
    {
      "updatedAt": "2026-08-19T18:36:02.461Z",
      "createdAt": "2026-08-19T18:36:02.461Z",
      "role": "workflow:owner",
      "workflowId": "cCt9O4NyNPyegHBq",
      "projectId": "cIpOMqhSXz7mHAJj",
      "project": {
        "updatedAt": "2026-07-23T14:53:00.343Z",
        "createdAt": "2026-07-23T14:06:58.898Z",
        "id": "cIpOMqhSXz7mHAJj",
        "name": "János Tóth <janos.toth.2000@gmail.com>",
        "type": "personal",
        "icon": null,
        "description": null,
        "creatorId": "392cafdf-bd25-4c0b-8d4a-e40c58c63156"
      }
    }
  ],
  "versionMetadata": {
    "name": null,
    "description": null
  }
}